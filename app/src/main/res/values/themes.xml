<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="Base.Theme.Pet" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/bg_for_register_form</item>
        <item name="colorOutlineVariant">@color/background_activity_register</item>
        <item name="colorOnPrimary">@color/white</item>
    </style>

    <style name="Theme.Pet" parent="Base.Theme.Pet" />
</resources>